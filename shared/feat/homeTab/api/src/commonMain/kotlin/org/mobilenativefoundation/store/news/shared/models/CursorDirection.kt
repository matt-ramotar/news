package org.mobilenativefoundation.store.news.shared.models

enum class CursorDirection {
    NEWER, // Fetching newer items relative to the cursor
    OLDER, // Fetching older items relative to the cursor
}